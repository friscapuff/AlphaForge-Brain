[pytest]
pythonpath = src
addopts = -q --maxfail=1
# Register custom markers
markers =
	integration: marks tests as integration (deselect with '-m "not integration"')
# Removed invalid/legacy asyncio option causing warning; add back with correct key if async tests introduced.
