policy_version: "2025.10.13"
max_runs: 60
per_strategy_top: 6
pin_expiry_days: 90
waiver_required: true
audit_log_path: "zz_artifacts/retention_audit.log"
breach_log_path: "zz_artifacts/retention_breaches.log"
metric_labels:
  policy: default
  environment: production
full_run_assets:
  - name: journaling
    root: zz_artifacts/journaling
    policy_version: "2025.10.13"
    decision_ref: "Decision 2"
    retention:
      max_runs: 60
      per_strategy_top: 6
