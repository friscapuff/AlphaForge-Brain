## Added

- Versioned journaling contract (`journaling_contract.schema.json`) covering
  enriched `CompletedTrade` payloads and the new `JournalingAggregate`
  artifact.
- Contract validation test harness and example payload to support downstream
  consumers (Mind) and governance automation.
