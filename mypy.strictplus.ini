############################################################
# Mypy Strict-Plus Overlay (staged)
# This file duplicates baseline config + incremental flags.
# Promotion (TH13) merges these into mypy.ini.
############################################################

[mypy]
python_version = 3.11
plugins = pydantic.mypy
strict = True
warn_unused_configs = True
warn_redundant_casts = True
warn_unused_ignores = True
show_error_codes = True
ignore_missing_imports = True
namespace_packages = True
mypy_path = alphaforge-brain/src
exclude = (?x)(^src/|^tests/)
explicit_package_bases = True

# Strict-plus incremental flags
no_implicit_reexport = True
warn_unreachable = True
strict_equality = True
warn_return_any = True

# Suppress third-party typing noise during allow-fail phase
disable_error_code = import-untyped, import-not-found

# Future staged (evaluate after stub stabilization)
# disallow_untyped_calls = True
# disallow_untyped_defs = True
# disallow_any_unimported = True

[report]
# (Optional) JSON/ad hoc reports could be added here.
